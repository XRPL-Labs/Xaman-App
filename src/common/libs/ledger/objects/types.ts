import { Offer, Escrow, Check, Ticket, PayChannel, NFTokenOffer, URIToken, Delegate } from '.';

export type LedgerObjects = Offer | Escrow | Check | Ticket | PayChannel | NFTokenOffer | URIToken | Delegate;
